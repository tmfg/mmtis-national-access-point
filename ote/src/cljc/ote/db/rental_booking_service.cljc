(ns ote.db.rental-booking-service
  "Marker cljc mainly to create strong references between keywords and such.

  That is, this namespace allows for emulating the style of the rest of the application without forcing the use of the
  heavyweight constructs entirely. A shim, if you will.")